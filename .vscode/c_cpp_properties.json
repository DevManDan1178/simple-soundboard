{
  "version": 4,
  "configurations": [
    {
            "name": "Win32",
            "includePath": [
                "${workspaceFolder}/src",
                "${workspaceFolder}/external",
                "${workspaceFolder}/**"
            ]
        }
  ]
}