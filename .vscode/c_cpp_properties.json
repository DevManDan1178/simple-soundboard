{
  "version": 4,
  "configurations": [
    {
      "name": "Soundboard",
      "compileCommands": "${workspaceFolder}/build/compile_commands.json"
    },
    {
            "name": "Win32",
            "includePath": [
                "${workspaceFolder}/src",
                "${workspaceFolder}/external",
                "${workspaceFolder}/**"
            ]
        }
  ]
}